# Documentation Archive

This folder keeps stale or historical docs that are no longer the source of truth.

Use `docs/current-work-backlog.md` for the active cleanup list.
Use `docs/current-code-federation-audit.md` for the current federation audit.
Use archived dated plans only as background context.
