// Package federation implements the AmityVox instance-to-instance federation protocol.
// It handles instance discovery, signed payload exchange, and message synchronization
// between federated AmityVox instances. See docs/architecture.md Section 7 for the
// full protocol specification.
// This package will be implemented in v0.2.0.
package federation
